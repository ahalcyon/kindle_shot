# Windows 専用アプリなので作業ツリーは CRLF、リポジトリ内は LF に正規化する。
# これを置かないと index=LF / 作業ツリー=CRLF がずれ、全ファイルが常に
# 「変更済み」表示になり、CI の diff / format チェックも当てにならなくなる。
* text=auto eol=crlf

# シェルスクリプトは LF 固定。CRLF だと shebang に \r が混ざって
# 「bad interpreter」で実行できなくなる（git hook がこれに該当）。
.githooks/** text eol=lf
*.sh text eol=lf

# バイナリは改行変換の対象外にする
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.ttf binary
*.otf binary
*.woff binary
*.woff2 binary
*.zip binary
*.exe binary
*.dll binary
*.whl binary
