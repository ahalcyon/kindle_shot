# AGENTS.md

このリポジトリで AI エージェント（Claude Code 等）が作業するときの規約。
人間向けの手順は `README.md`（「セットアップ」「開発メモ」）を参照。

コミットメッセージ・PR 説明・コードコメント・issue・**利用者との対話**は日本語で書く。

ここには方針だけを書く。個別の事象・実測値・経緯は issue に残し、ここへは写さない。

## 目的と優先順位

**Kindle の蔵書から精度の高い PDF を作ること。** 作った PDF は、本文をコピーしてメモを取る・
一部を LLM に要約させるのに使う。したがって「全文を漏れなく」より
**「コピーした文字列が原文どおりか」**が効く。

- **時間と計算資源は惜しまない。** 「遅くなるから」を対策を弱める理由にしない。
  ただし上限の無い待ちは置かない。上限は必ず置き、根拠をコメントに書く
- **裏の取れていない対策は入れない。** 実機で確かめてから入れる。確かめられないなら入れずに issue に残す
- **黙って壊れるほうが、大声で落ちるより悪い。** 部分本・中抜け・誤ったテキスト層は、
  完成扱いで確定すると気づけない。対策が新しい欠け方を作っていないかを必ず見る
- **誤ったテキスト層は、テキスト層が無いより悪い**

## 次に何をやるかは自分で決める

issue を片付けたら、次の issue も自分で選んで始める。「次はどれを」と聞かない。
全体の工程と現在地は issue #93（ロードマップ）にあり、進捗があったら更新する。

優先順位:

1. 蔵書に実害が残っているもの（部分本・中抜け・誤読）
2. 黙って壊れるもの > 大声で落ちるもの
3. 原因がはっきりしているもの > 調査から始まるもの
4. 本番が通る経路（headless）> 通らない経路（画面キャプチャ）

利用者に聞くのは次の場合だけ。進捗は、許可を求めるためではなく何をしたか分かるように報告する。

- 蔵書のファイルを消す・上書きするとき
- 方針そのものを変えるとき
- 実機で確かめようがなく、判断が分かれるとき

## 本ごとに形式を分ける

全冊を一律の `--format` で回さない。

| 本の種類 | 形式 |
|---|---|
| 漫画・図版が主体の本 | `image_pdf`（OCR をかけない） |
| 文章の本 | `searchable_pdf`（品質優先。前処理・誤読辞書・点検を通す） |

判定は勘ではなく実測で行う（`core/book_format.py`）。**確信があるときだけ image_pdf に倒し、迷ったら
searchable_pdf。** image_pdf の誤りは撮り直しが要るが、searchable_pdf の誤りはテキスト層を剥がせば済む。
結果は `books.json` の `format` に書く。

## 作業の流れ

1. **ブランチ** — issue に紐づく作業は `feature/<issue番号>-<短い英語>`。master に直接コミットしない。
   issue に紐づかない作業は `docs/...` / `chore/...`（番号なし）
2. **コミット** — 件名は `<type>(#<番号>): <要約>`（Conventional Commits）。本文には
   「なぜそうしたか」（根拠・採らなかった案・検証内容）を書き、末尾に `Refs #<番号>`。
   整形だけの変更と挙動を変える変更は分ける
3. **変更に伴って直すもの** — 挙動を変えたらテストを足す（GUI 層は対象外）。
   CLI のオプション・終了コード・停止理由・設定項目を変えたら README も直す
4. **検証** — `README.md`「開発メモ > 検証コマンド」の lint / format / type check / test を通す。
   テストには Windows が要る。回せなかったら未検証の範囲を PR に書き、利用者に実行を頼む。
   挙動を守るテストは、実装を壊すと落ちることを確かめる
5. **実機スモーク** — キャプチャ経路に触ったら push 前に実機で確かめる（下記）
6. **push と PR** — 検証 → 実機スモーク → コミット → **push + PR 作成** → レビュー依頼 → 指摘を追加コミット、の順。
   レビューを待たずに PR を作り、CI とレビューを並行させる。
   本文に `Closes #<番号>`、変更の理由（挙動を変えていないならその根拠）、検証コマンドと結果、
   レビューの指摘と対応、既知の制約を書く
7. **レビュー** — 独立したレビュー担当（サブエージェント）に `origin/master...HEAD` を見せる（2 回目以降は前回以降の差分でよい）。
   依頼の前に自分で差分を読み返す。差分・背景・検証済みの事実・確認観点を具体的に渡す（探索させない）。
   指摘は must fix / should fix / nits に分けさせ、事実確認してから採否を決める。見送る理由は PR に書く。
   **レビュー担当に `gh pr checkout` を使わせない**（同じ作業ツリーのブランチが切り替わり、その後の
   コミットが別ブランチに乗る。差分は `gh pr diff` で読む）
8. **CI** — PR を作ったらすぐ `python3 scripts/watch_ci.py` を**ハーネスが追跡するバックグラウンド実行**で
   起動する（シェルの `&` は通知が来ない）。`gh pr merge --auto` は使わない

### 実機スモーク

自動テストも CI もキャプチャの実機層をカバーしない。触ったら実機で流す。

```
python scripts/smoke_capture.py            # headless。画面を占有しない
python scripts/smoke_capture.py --screen   # 画面キャプチャ経路。デスクトップを占有する
```

- **どのファイルがどちらで確かめられるかは `.githooks/pre-push` が正典。** ここには写さない
- pre-push フックが強制するのは headless だけ。画面キャプチャ経路は本番で通らず、
  検証にデスクトップが要るのでゲートにしていない。**触ったら `--screen` を手で流す**
- **確かめるのは、変更したコードが実際に走る経路。** 別経路のスモークが緑でも保証にならない
- PR には実行したコマンドと `manifest.json` の要点（`total_pages` / `stopped_reason` / `duration_seconds`）を貼る
- 実機で確かめられなかったら、PR にその旨と未検証の範囲を書く。`--no-verify` を常態化させない（原因が環境側なら直す）
- 対象は Kindle Cloud Reader（`kindle_cloud`）。PC アプリはプログラムから本を開けないので使わない
- 先頭への巻き戻しは Kindle の読書位置を動かす

フックの設定（クローンごとに 1 回）:

```
git config core.hooksPath .githooks
git config kindleshot.smokeAsin B0XXXXXXXX
```

使われる Python は `.githooks/pre-push --check` で確認できる。

## 本番バッチの運用

`cli.py batch` は都度の確認なしに回してよい。ただし次を守る。守れないなら回さない。

- **本番の出力先は `../kindle_shot_run/library/` に一本化する。** 別の出力先を増やさない
- **精度改善の途中で全冊を回さない。** 種類ごとの抜き取り（`--max-pages` 付き）で測る。抜き取りの出力は
  `library/` に混ぜない（batch は PDF がある本を飛ばすので、部分 PDF が完成扱いになる）。全冊は精度が頭打ちになってから
- **中間生成ファイルを溜めない。** 本番で `--keep-images` を付けない。失敗した本の中間ファイルは意図的に残るので、
  原因を確かめたら手で消す。調査用の一時ファイルはその場で消す
- **ディスクの空きを見ながら進める。** 実行前と実行中に見て、細っていくなら止めて報告する。尽きてから報告するのは失敗
- **結果は数字で残す。** 成功・非対応・失敗の内訳、所要時間、ディスク使用量。失敗は理由ごとに分けて issue にする
- **終了コード 0 でも読めるとは限らない。** `scripts/check_readability.py` と
  `scripts/audit_rules.py`（誤読辞書を足したとき）で点検する。どちらも見えるのは一部の誤りだけで、ゲートではない
- **誤読辞書の表記ゆれの正規化は、同じ本の中で数えてから入れる。** コーパス全体の多数決で決めない
- **止めたつもりのプロセスが生きていないかを、Windows 側で確かめる。** WSL から `cmd.exe` 経由で起動した
  本番プロセスは、WSL 側の親を止めても Windows 側の python は死なない。止めるときは
  `Get-CimInstance Win32_Process` でスクリプト名（`check_format.py` / `capture_app_books.py` など）を探して
  `Stop-Process` し、再開の前にも同じ確認をする。生き残った旧版と新版が同じ CSV に同時に追記し、
  行が混ざって壊れた（2026-09-19、#96）

## 調査と報告の原則

- **検証していないことを事実として書かない。** 環境・権限・原因は、コマンドを流してから書く。
  確かめていなければ「未確認」と書く。結果待ちの間に結論を先取りしない
- **報告は実行結果から組み立てる。** 実行したコマンド・戻り値・件数をそのまま載せる。
  表示やログを、引数やフラグから組み立てない。誤りが分かったら issue 本文・コメントを実測値で直す
- **単発の観測を対策の根拠にしない。** 通しで最後まで回して確かめる。
  効かないと分かったことも issue に残す
- **「本が進んだ」は画面ではなく読書位置で判定する。** 画面は再描画やアニメーションでも変わる。
  ただし位置の表示形式が途中で変わる本もあるので、値の意味を確かめる
- **長い処理を始める前に全体量を測る。** 他の本から外挿しない。数字が食い違うときは、
  それぞれ違うものを指している可能性を疑う
- **見積もりは根拠を添える。** 終了時刻はその場で `date` を見てから書く。
  所要時間の母集団（何を含めたか）を言葉にし、割り算を見せる
- **待つときは待ち合わせを起動してからターンを終える。** 「待ちます」と書くだけでは何も再開しない
- **利用者から指摘を受けたら、方針としてこのファイルに書いて PR を出す。** 会話の中だけで済ませない。
  書くのは方針だけで、経緯は issue に残す
- **実装をわざと壊す確認（変異テスト）の前にバックアップを取る。** `git checkout <file>` で戻すと、
  未コミットの修正ごと消える。消してしまったら「たぶん戻った」で進めず作り直す
- **一時的なスクリプトや実験の出力はリポジトリの外に置く**（スクラッチ領域や `%TEMP%`）

## Kindle Cloud Reader について分かっていること

- ページは**サーバーが描画**し、リーダーは数ページずつ受け取って並べる。サーバーが描けない
  ページがある本では、リーダーはその先を表示できない。**描けない箇所を飛ばして完成扱いにしない**
- `https://read.amazon.co.jp/kp/kindle-reader` は無料サンプルを読む経路で、本の一部しか読めない。撮影に使わない
- 描画の応答には各ページの位置範囲と本全体の範囲が入っている。全体量や抜けの確認には、
  フッターの表示よりこちらが確実

## このリポジトリ固有の注意

- **Windows 専用。** Win32 API・Windows のフォントに依存する。テストは Windows で回す（CI も windows-latest）
- **改行コード**は「リポジトリ内 LF / 作業ツリー CRLF」。WSL から書き換えるときは `newline=""` で読み書きして元の改行を保つ
- **`core/config.json` は利用者のローカル設定。** テストから読み書きしない（`tests/conftest.py` の `isolated_config` を使う）
- `gh` は WSL 側にしか無い。`scripts/watch_ci.py` は WSL の `python3` で実行する
